{
	"name": "sveltekit-statefull-session",
	"version": "0.0.2",
	"license": "MIT",
	"type": "module",
	"scripts": {
		"dev": "pnpm -r --filter \"@sveltekit-statefull-session/*\" --parallel dev",
		"build": "pnpm -r --filter \"@sveltekit-statefull-session/*\" build",
		"example": "pnpm --filter simple-session dev",
		"vercel": "pnpm build && pnpm --filter simple-session build",
		"release:prepare": "changeset && changeset version"
	},
	"peerDependencies": {
		"svelte": "^3.54.0"
	},
	"devDependencies": {
		"@changesets/cli": "^2.9.2",
		"typescript": "^5.0.0",
		"vitest": "^0.25.3"
	}
}